# Comprehensive Database Objects (DBOs) and Pathfinding Algorithm Analysis

## Database Objects (DBOs) Overview

### Core Database Structure

The **TrainControllerSystem** database uses **Entity Framework Core 10.0** with SQL Server integration. Here's how the DBOs are structured and integrated:

#### Primary Entity Tables
1. **Sections** (`Sections`) - Track divisions with subsections
2. **SubSections** (`SubSections`) - Granular track segments with speed limits
3. **Platforms** (`Platforms`) - Station platforms with station and subsection mapping
4. **Switches** (`Switches`) - Railway switch control points
5. **Trains** (`Trains`) - Train registry and current status
6. **Stations** (`Stations`) - Station information
7. **Objects** (`Objects`) - Track objects (signals, sensors) linked to subsections

#### Timetable System
- **TimetableEntries** - Active train schedules
- **TimetableEntriesArrived** - Completed journeys
- **TimetableEntriesUpcoming** - Pending departures

#### Critical Database Views for Pathfinding

##### 1. **V_Lookup_Section_NextSection**
**Purpose**: Core routing table with pre-computed paths and switch constraints
```sql
-- Key fields:
- Section_DB_ID -> NextSection_DB_ID (Routing)
- Direction (0/1 for bidirectional tracks)
- Destinations (Comma-separated platform IDs)
- SwitchConstraints (e.g., "=SW1=straight,=SW2=turned")
```

**Integration**: Used by `RailwayPathfinderService.cs:165` for pathfinding queries

##### 2. **V_PlatformEntry**
**Purpose**: Platform-level routing with section locking information
```sql
-- Key fields:
- Platform_DB_ID, Section_DB_ID, NextSection_DB_ID
- Route (Format: "#1#2#3#")
- SectionsToLock (Conflict prevention)
- Level (Hierarchical routing)
```

**Integration**: Platform entry/exit path calculation in `TrackGraph.cs:46`

#### Lookup Tables (Pathfinding Infrastructure)
- **Lookup_Section_NextSection** - Basic section connections
- **Lookup_SectionNextSection_Switches** - Switch constraints
- **Lookup_SectionNextSection_Destinations** - Route destinations
- **Lookup_Sections_SubSections** - Section composition
- **Lookup_SectionNextSectionNextSection** - Multi-section routing

## Pathfinding Algorithm Breakdown

### Architecture Overview
The pathfinding system uses a **multi-layered approach**:

1. **TrackGraph** - Graph-based representation using subsections as nodes
2. **RailwayPathfinderService** - High-level pathfinding with conflict resolution
3. **RoutePlannerService** - Platform-to-platform routing with switch configuration

### Core Pathfinding Algorithm

#### Phase 1: Graph Initialization (`TrackGraph.cs:30-92`)
```csharp
// Load subsections as nodes
var subsections = await dbContext.SubSections.ToListAsync();
foreach (var subsection in subsections)
{
    graph.AddNode(subsection.DB_ID, subsection.Name, subsection.AllowedSpeed);
}

// Load track connections from database view
var connections = await dbContext.VLookupSectionNextSection
    .FromSqlRaw("SELECT * FROM V_Lookup_Section_NextSection")
    .ToListAsync();
```

#### Phase 2: Platform Mapping (`TrackGraph.cs:285-320`)
```csharp
// Map platforms to subsections for station-level routing
private void MapPlatformToNode(string stationName, string platformName, int nodeId)
{
    var keys = new[] {
        $"{normalizedStation}-{normalizedPlatform}",
        $"{stationName.ToLower()}-{platformName.ToLower()}"
        // Multiple variations for robustness
    };
    foreach (var key in keys)
    {
        _platformToNodeId[key] = nodeId;
    }
}
```

#### Phase 3: BFS Pathfinding (`TrackGraph.cs:432-501`)
**Algorithm**: Breadth-First Search for optimal shortest path

```csharp
private RoutePlan FindRouteBFS(int sourceId, int destId)
{
    var visited = new HashSet<int>();
    var queue = new Queue<QueueItem>();
    var parent = new Dictionary<int, EdgeInfo>();

    queue.Enqueue(new QueueItem { NodeId = sourceId, Path = new List<EdgeInfo>() });
    visited.Add(sourceId);

    while (queue.Count > 0)
    {
        var current = queue.Dequeue();

        if (current.NodeId == destId)
        {
            // Reconstruct path from parent mapping
            var path = new List<EdgeInfo>();
            var node = destId;
            while (parent.ContainsKey(node) && node != sourceId)
            {
                path.Insert(0, parent[node]);
                node = parent[node].SourceNodeId;
            }
            return new RoutePlan { Path = path, TotalLength = path.Sum(e => e.Length) };
        }

        // Explore neighbors
        foreach (var edge in _edges[current.NodeId])
        {
            if (!visited.Contains(edge.TargetId))
            {
                visited.Add(edge.TargetId);
                parent[edge.TargetId] = edgeInfo;
                queue.Enqueue(new QueueItem { NodeId = edge.TargetId, Path = newPath });
            }
        }
    }
}
```

#### Phase 4: Conflict Resolution (`RailwayPathfinderService.cs:79-153`)

**Multi-Train Conflict Prevention**:
1. **Opposing Direction Conflicts** (`HasOpposingTrainConflict`):
```csharp
return otherTrains
    .Where(x => x.Direction != train.Direction && x.DB_ID != train.DB_ID)
    .Any(x => x.LockedSections.Contains(sectionId) &&
             (x.LockedSections.IndexOf(sectionId) < x.LockedSections.IndexOf(x.Destination?.SubSection_DB_ID ?? -1)));
```

2. **Same Direction Conflicts** (`HasSameDirectionConflict`):
```csharp
return otherTrains
    .Where(x => x.DB_ID != train.DB_ID && x.Direction == train.Direction && x.State == TrainState.Moving)
    .Any(x => x.LockedSections.Contains(sectionId) || x.SubSection?.DB_ID == sectionId);
```

#### Phase 5: Switch Configuration (`RoutePlannerService.cs:167-254`)

**MQTT Switch Control Integration**:
```csharp
foreach (var switchConfig in switchConfigurations)
{
    // Check if already configured
    if (IsSwitchAlreadyConfigured(switchConfig.SwitchName, switchConfig.Position))
        continue;

    // Send via MQTT
    var success = await _adminMQTTService.SendSwitchCommandAsync(switchConfig.SwitchName, switchConfig.Position);

    // Track state to prevent redundant commands
    lock (_lockObject)
    {
        _lastSwitchStates[switchConfig.SwitchName] = position;
    }
}
```

### Pathfinding Optimizations

#### 1. **Database View Optimization**
- Pre-computed routes in `V_Lookup_Section_NextSection`
- Switch constraints aggregated for faster processing
- Destination platforms embedded for route validation

#### 2. **Platform-Centric Routing**
- Platform-to-platform navigation instead of section-to-section
- Station entry/exit path pre-computation
- Hungarian station name normalization (`felso`/`felső`, `also`/`alsó`)

#### 3. **Section Locking System**
```csharp
// Section locking prevents collisions
train.LockedSections = calculatedPath; // Atomic operation
```

#### 4. **Path Smoothing** (`PathOptimizer.cs:529-554`)
```csharp
public static List<int> OptimizePath(List<int> originalPath, TrackGraph trackGraph)
{
    // Remove unnecessary intermediate sections
    for (int i = 1; i < originalPath.Count - 1; i++)
    {
        var current = originalPath[i - 1];
        var next = originalPath[i + 1];
        var hasDirectConnection = trackGraph.HasDirectConnection(current, next);

        if (!hasDirectConnection)
            optimized.Add(originalPath[i]);
    }
}
```

### MQTT Integration with DBOs

#### MQTT Topics for Hardware Control
- **`track/command/signal`** - Signal control commands
- **`train/signal/request`** - Train signal operation requests
- **`train/signal/response`** - Train signal operation responses
- **`train/signal/changed`** - Signal state change notifications

#### Real-Time Database Updates
```csharp
// Track feedback updates section status
mqttClient.SubscribeAsync("rocrail/service/info/fb");

// Section occupancy triggers database updates
await dbContext.SaveChangesAsync();
```

### Performance Characteristics

#### 1. **Database Efficiency**
- **Views** reduce complex joins to single-table queries
- **Lookup tables** provide O(1) access for common routing queries
- **Indexing** on Section_DB_ID, Platform_DB_ID for fast lookups

#### 2. **Memory Optimization**
- **Lazy loading** of navigation properties
- **Dictionary caching** for frequently accessed mappings
- **Connection pooling** via EF Core

#### 3. **Real-Time Performance**
- **BFS Algorithm**: O(V + E) time complexity for pathfinding
- **Section locking**: O(1) conflict detection
- **Switch configuration**: Asynchronous MQTT with state caching

This pathfinding system provides **collision-free, optimal routing** for multiple trains with **real-time switch control** through MQTT integration, all backed by a sophisticated SQL Server database schema with optimized views for high-performance railway operations.

---

Generated on: 2025-12-05
System: .NET MAUI Train Control System
Database: SQL Server TrainControllerSystem